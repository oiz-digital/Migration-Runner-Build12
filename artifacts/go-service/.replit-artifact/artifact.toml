kind = "web"
previewPath = "/go-service/"
title = "CryptoX Go Service"
version = "1.0.0"
id = "artifacts/go-service"
router = "path"

[[services]]
name = "web"
paths = [ "/go-service/", "/go-service/healthz", "/go-service/ws" ]
localPort = 23004

[services.development]
run = "go run ."

[services.production]

[services.production.build]
args = ["sh", "-c", "mkdir -p artifacts/go-service/dist && cd artifacts/go-service && CGO_ENABLED=0 go build -ldflags='-s -w' -o dist/server ."]

[services.production.run]
args = ["./artifacts/go-service/dist/server"]

[services.production.run.env]
PORT = "23004"
BASE_PATH = "/go-service/"

[services.production.health.startup]
path = "/go-service/"
